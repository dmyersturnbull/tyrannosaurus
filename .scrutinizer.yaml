build:
  environment:
    # :tyranno: python: "${.project.requires-python~.semver_max(@)~}"
    python: "3.11.4"
  nodes:
    my-tests:
      dependencies:
        before:
          - "pip install 'hatchling~=1.7'"
    analysis:
      tests:
        override:
          - py-scrutinizer-run
checks:
  python:
    code_rating: true
    duplicate_code: true
